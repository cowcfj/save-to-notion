/**
 * Content-safe extraction messages.
 *
 * 僅保留 content extraction fallback path 需要的 user-facing 訊息。
 */
export const EXTRACTION_FALLBACK_MESSAGES = Object.freeze({
  EMPTY_FALLBACK: '擷取內容失敗。頁面可能為空白或受保護。',
  ERROR_FALLBACK: '擷取發生錯誤，請稍後再試。',
});
