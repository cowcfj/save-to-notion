/**
 * Content-safe extraction messages.
 *
 * 僅保留 content extraction fallback path 需要的 user-facing 訊息。
 */
export const CONTENT_EXTRACTION_MESSAGES = Object.freeze({
  EMPTY_FALLBACK: '內容提取失敗。此頁面可能為空白或受保護。',
  ERROR_FALLBACK: '內容提取發生錯誤，請稍後再試。',
});
