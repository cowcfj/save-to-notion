name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  workflow_dispatch:

jobs:
  test:
    name: Test & Build
    if: ${{ !startsWith(github.head_ref, 'release-please--') }}
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    strategy:
      matrix:
        node-version: [20.x]

    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          fetch-depth: 1

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@6044e13b5dc448c55e2357c09f80417699197238 # v6
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Upgrade npm
        run: npm install -g npm@11.8.0 --ignore-scripts

      - name: Install dependencies
        env:
          PUPPETEER_SKIP_CHROMIUM_DOWNLOAD: 'true'
          PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD: '1'
        run: npm ci --ignore-scripts

      - name: Enforce minimatch security floor (ReDoS guard)
        run: |
          node <<'NODE'
          const fs = require('fs');

          const lock = JSON.parse(fs.readFileSync('package-lock.json', 'utf8'));
          const packages = lock.packages || {};

          const parseVersion = version =>
            String(version)
              .split('.')
              .slice(0, 3)
              .map(part => Number.parseInt(part, 10) || 0);

          const compareVersion = (a, b) => {
            const pa = parseVersion(a);
            const pb = parseVersion(b);
            for (let index = 0; index < 3; index += 1) {
              if (pa[index] > pb[index]) return 1;
              if (pa[index] < pb[index]) return -1;
            }
            return 0;
          };

          const isVulnerable = version => {
            if (compareVersion(version, '3.1.3') < 0) return true;
            if (compareVersion(version, '9.0.0') >= 0 && compareVersion(version, '9.0.7') < 0) return true;
            if (compareVersion(version, '10.0.0') >= 0 && compareVersion(version, '10.2.3') < 0) return true;
            return false;
          };

          const minimatchEntries = Object.entries(packages)
            .filter(([packagePath, meta]) => packagePath.includes('node_modules/minimatch') && meta?.version)
            .map(([packagePath, meta]) => ({ packagePath, version: meta.version }));

          const vulnerable = minimatchEntries.filter(entry => isVulnerable(entry.version));

          if (minimatchEntries.length === 0) {
            console.warn('⚠️ 未在 package-lock.json 中找到 minimatch，略過版本檢查。');
            process.exit(0);
          }

          const uniqueVersions = [...new Set(minimatchEntries.map(entry => entry.version))].sort((a, b) =>
            compareVersion(a, b)
          );
          console.log(`✅ minimatch versions in lockfile: ${uniqueVersions.join(', ')}`);

          if (vulnerable.length > 0) {
            console.error('❌ 發現 vulnerable minimatch 版本：');
            for (const entry of vulnerable) {
              console.error(`- ${entry.packagePath}: ${entry.version}`);
            }
            process.exit(1);
          }

          console.log('✅ minimatch 版本均符合安全下限。');
          NODE

      - name: Node.js 20.x compatibility check
        run: |
          echo "Node.js version: $(node --version)"
          echo "npm version: $(npm --version)"
          echo "Jest version: $(npm list jest | grep jest)"

      - name: Lint (non-blocking)
        run: npm run lint
        continue-on-error: true

      # 注意：Jest 測試已從 CI 移除，改為本地執行
      # 原因：配合 E2E 測試的 headed 模式要求，統一在本地運行完整測試
      # 開發者需在提交前運行 npm run test:all 生成覆蓋率報告

      # 注意：E2E 測試已從 CI 移除
      # 原因：Chrome headless 模式不支持 Extension Service Worker（Chromium Issue #41309918）
      # E2E 測試可在本地使用 npm run test:e2e 運行（需要 headed 模式）

      - name: Verify Production Build
        run: |
          echo "Building production bundle..."
          npm run build:prod

          # Verify bundle existence (content + highlighter merged bundle)
          if [ ! -f "dist/content.bundle.js" ]; then
            echo "❌ Content bundle file not found!"
            exit 1
          fi

          if [ ! -f "dist/scripts/background.js" ]; then
            echo "❌ Background script not found!"
            exit 1
          fi

          # Check bundle size (merged content+highlighter bundle, fail if > 200KB)
          SIZE=$(stat -f%z "dist/content.bundle.js" 2>/dev/null || stat -c%s "dist/content.bundle.js" 2>/dev/null)
          echo "✅ Content bundle size: $SIZE bytes"

          if [ "$SIZE" -gt 204800 ]; then
            echo "❌ Bundle size ($SIZE bytes) exceeds 200KB limit!"
            exit 1
          fi

      - name: Validate coverage file
        id: validate-coverage
        run: |
          LCOV_FILE=""
          if [ -f "coverage/merged/lcov.info" ]; then
            LCOV_FILE="coverage/merged/lcov.info"
            echo "Found merged coverage file."
          elif [ -f "coverage/jest/lcov.info" ]; then
            LCOV_FILE="coverage/jest/lcov.info"
            echo "Found jest coverage file."
          else
            echo "⚠️ Coverage file not found in merged or jest directories."
            echo "valid=false" >> $GITHUB_OUTPUT
            exit 0
          fi

          # 檢查文件是否包含有效的 LCOV 格式（至少包含 SF: 和 end_of_record）
          if grep -q "^SF:" "$LCOV_FILE" && grep -q "end_of_record" "$LCOV_FILE"; then
            LINE_COUNT=$(wc -l < "$LCOV_FILE")
            echo "✅ Valid LCOV file detected ($LINE_COUNT lines)"
            echo "valid=true" >> $GITHUB_OUTPUT
            echo "file=$LCOV_FILE" >> $GITHUB_OUTPUT
          else
            echo "⚠️ Invalid LCOV format in $LCOV_FILE"
            echo "valid=false" >> $GITHUB_OUTPUT
          fi

      - name: Upload coverage to Codecov
        if: steps.validate-coverage.outputs.valid == 'true'
        uses: codecov/codecov-action@671740ac38dd9b0130fbe1cec585b89eea48d3de # v5
        with:
          files: ${{ steps.validate-coverage.outputs.file }}
          flags: combined
          fail_ci_if_error: false
          use_oidc: true

      - name: Upload Test Artifacts (Action Report)
        if: always()
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        with:
          name: junit-report
          path: junit.xml
          if-no-files-found: ignore
